#!/usr/bin/expect
#서버로부터 <dir path> 디렉토리를 현재 위치로 다운받음
#expect -f recv_file.exp <dir path>

set dir [lindex $argv 0]

spawn rsync -avzhP ubuntu@13.125.216.169:~/$dir .
expect -re "password"
send "dudgh123\r"

interact
